# ── Bulk data and models ────────────────────────────────────────────────────
# Too large for the repo. The case-study sessions are what manuscript_figures/
# reads to rebuild Figures 2-4.
models/
case_study_1/
case_study_2/
case_study_3/
cp3_vs_cp4/
box_prompt_benchmark/

# ── Manuscript ──────────────────────────────────────────────────────────────
# The paper drafts, and everything that builds their figures. Both are anchored
# with a leading slash so they only match at the repo root.
/manuscript/
/manuscript_figures/

# ── Figures ─────────────────────────────────────────────────────────────────
# Working files for the docs pipeline figure: the per-step SVGs, the build script
# and the manuscript export. Only docs/pipeline_overview_correct.svg is published.
docs/figure_sources/
docs/pipeline_overview_correct_manuscript.png

# Members model_inference_example.ipynb unpacks from example_session.zip
notebooks/example_session/

# ── Environments and secrets ────────────────────────────────────────────────
.env
.envrc
.venv
env/
venv/
ENV/
.streamlit/secrets.toml

# ── Tooling caches and build output ─────────────────────────────────────────
__pycache__/
*.py[codz]
*$py.class
*.so
build/
dist/
*.egg-info/
*.egg
.eggs/
.ipynb_checkpoints
.ruff_cache/
.mypy_cache/
.pytest_cache/
.coverage
.coverage.*
htmlcov/

# ── Editor, OS and agent scratch ────────────────────────────────────────────
.claude/
.agents/skills/developing-with-streamlit
*.DS_Store
*.bak
*.log
*.npy
